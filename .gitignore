# Design sources (Affinity files, icon exports) — not needed to run the app
Affinity/
Icones/

# OS / editor junk
.DS_Store
Thumbs.db
*.tmp

# NOTE: backend/locker-worker.js IS versioned on purpose — it is the Cloudflare
# Worker source used by the optional Locker, contains no secrets (everything is
# read from environment variables/bindings), and is part of the setup procedure.
